services:
  zazi:
    build: .
    container_name: zazi
    ports:
      - "4000:4000"
    volumes:
      - zazi-data:/data
    restart: unless-stopped
    environment:
      - PORT=4000
      - HOST=0.0.0.0
      - ZAZI_DB_PATH=/data/zazi.sqlite

volumes:
  zazi-data:
